# Source recordings — large, and not needed to build the manual.
# The frames used as figures have already been extracted into manual_assets/.
1_SCREEN_RECORDINGS/

# Scratch notes
Untitled.md

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Editors
.idea/
.vscode/
*.swp

# NOTE: docs/ is deliberately NOT ignored. It holds the built site
# (index.html + the two PDFs) and is what GitHub Pages serves.
